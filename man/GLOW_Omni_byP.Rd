% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GLOW_Omni.R
\name{GLOW_Omni_byP}
\alias{GLOW_Omni_byP}
\title{GLOW Omnibus Test from Pre-Computed P-values}
\usage{
GLOW_Omni_byP(
  Pvalues,
  Zsigns,
  M,
  Bstar,
  PI,
  test_specs = NULL,
  include_snv_cct = TRUE,
  return_weights = FALSE,
  ...
)
}
\arguments{
\item{Pvalues}{Numeric vector of variant-level p-values (length m, in
\eqn{[0,1]}).}

\item{Zsigns}{Numeric vector of effect direction signs (length m, +1 or -1).}

\item{M}{Correlation matrix of Z-scores (m x m).}

\item{Bstar}{Numeric vector of scaled effect sizes (length m).}

\item{PI}{Numeric vector of variant-importance scores (length m, in \eqn{[0,1]}).}

\item{test_specs}{List of test spec lists (default: NULL uses
\code{\link{default_test_specs}}).}

\item{include_snv_cct}{Logical (default TRUE). Append SNV-level and final
omnibus CCT rows.}

\item{return_weights}{Logical (default FALSE). If TRUE, attach the
internally-assembled weight matrix to the result (forwarded from
\code{run_bsf_tests}).}

\item{...}{Additional arguments passed to \code{run_bsf_tests}.}
}
\value{
Same structure as \code{\link{GLOW_Omni}}.
}
\description{
Performs the GLOW omnibus test when individual variant p-values are already
computed. Reconstructs Z-scores from p-values and their signs, then runs
the omnibus test pipeline via \code{run_bsf_tests()}.
}
\details{
\strong{Algorithm}:
\enumerate{
\item Reconstruct Z-scores: \eqn{Z = \Phi^{-1}(1 - p/2) \times sign}
\item Call \code{run_bsf_tests()} with reconstructed Z-scores
}

\strong{CRITICAL}: The correlation matrix M must reflect the Z-score correlation
structure, not the adjusted p-value structure.
}
\seealso{
\code{\link{GLOW_Omni}} for omnibus test with Z-scores
\code{\link{getZ_marg_score_binary_SPA}} for SPA-adjusted p-values
}
