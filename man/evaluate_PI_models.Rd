% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_PI_utils.R
\name{evaluate_PI_models}
\alias{evaluate_PI_models}
\title{Evaluate PI Model Performance}
\usage{
evaluate_PI_models(
  models,
  cases,
  controls,
  model_type = "auto",
  features = NULL,
  max_controls = NULL,
  n_eval_controls = NULL,
  random_seed = NULL
)
}
\arguments{
\item{models}{List of model objects, or output from \code{load_PI_models()}.}

\item{cases}{Matrix, data.frame, or file path to case (positive) annotations.}

\item{controls}{Matrix, data.frame, file path, or directory for control
(negative) annotations. If a directory, loads all chr*.csv files.}

\item{model_type}{Character. "auto" (detect), "LASSO", or "GLM".}

\item{features}{Character vector. Feature columns to use. NULL uses default
11 PI features.}

\item{max_controls}{Integer. Maximum controls to load from directory (memory
limit). Uses proportional sampling per chromosome. NULL loads all.}

\item{n_eval_controls}{Integer. Number of controls to use in evaluation after
loading. NULL uses all loaded controls.}

\item{random_seed}{Integer. For reproducible control sampling.}
}
\value{
List with components:
\item{per_model}{Data frame with model_id and auc columns}
\item{ensemble}{List with auc and predictions}
\item{summary}{List with mean_auc, sd_auc, min_auc, max_auc}
\item{roc_data}{List with labels, per-model predictions, ensemble predictions}
\item{metadata}{Evaluation settings and data info}
}
\description{
Evaluates the discriminative performance of PI models using AUC (Area Under
the ROC Curve). Computes per-model AUC and ensemble AUC on case/control data.
}
\examples{
\dontrun{
models <- load_PI_models("path/to/piModels/")
eval_result <- evaluate_PI_models(
  models,
  cases = "cases_annotated.csv",
  controls = "controls_dir/",
  max_controls = 10000
)
cat("Ensemble AUC:", eval_result$ensemble$auc, "\n")
}

}
