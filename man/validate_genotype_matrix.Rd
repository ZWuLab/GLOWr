% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_inputs.R
\name{validate_genotype_matrix}
\alias{validate_genotype_matrix}
\title{Validate genotype matrix for GLOW tests}
\usage{
validate_genotype_matrix(G, allow_na = FALSE, discrete = TRUE, min_sd = 1e-06)
}
\arguments{
\item{G}{Genotype matrix (samples x variants). Rows are individuals,
columns are genetic variants. Expected encoding: 0/1/2 for discrete
genotypes, or \eqn{[0,2]} for dosages.}

\item{allow_na}{Logical; if TRUE, allow missing values (NA). Default: FALSE}

\item{discrete}{Logical; if TRUE, require discrete 0/1/2 encoding.
If FALSE, allow continuous dosages in \eqn{[0,2]}. Default: TRUE}

\item{min_sd}{Minimum standard deviation required for at least one variant.
Ensures sufficient genetic variation. Default: 1e-6}
}
\value{
A list with elements:
\describe{
\item{valid}{Logical; TRUE if all checks pass}
\item{message}{Character string with validation result or error message}
\item{n_samples}{Number of samples (rows)}
\item{n_variants}{Number of variants (columns)}
\item{n_missing}{Number of missing values (if allow_na=TRUE)}
\item{n_monomorphic}{Number of monomorphic variants (SD < min_sd)}
\item{warnings}{Character vector of non-fatal warnings}
}
}
\description{
This function performs comprehensive validation of genotype data to ensure
it meets the requirements for GLOW variant-set tests. It checks:
\itemize{
\item Matrix structure (non-empty, numeric)
\item Missing values (optionally allowed or disallowed)
\item Genotype encoding (0/1/2 for additive, or continuous dosages)
\item Variant variation (at least some variants must be polymorphic)
}
}
\details{
Checks that a genotype matrix meets basic requirements for variant-set
association testing. This includes checking dimensions, missing values,
valid genotype encoding, and sufficient variation.

\strong{Validation checks performed}:
\enumerate{
\item \strong{Matrix structure}:
\itemize{
\item Must be a matrix or coercible to matrix
\item Must have at least 1 sample and 1 variant
\item Must contain numeric values
}
\item \strong{Missing values}:
\itemize{
\item If allow_na=FALSE: No missing values allowed
\item If allow_na=TRUE: Missing values are permitted but counted
}
\item \strong{Genotype encoding}:
\itemize{
\item If discrete=TRUE: All non-NA values must be in {0, 1, 2}
\item If discrete=FALSE: Any numeric values in \eqn{[0, 2]} are allowed (dosages)
}
\item \strong{Variation check}:
\itemize{
\item At least one variant must have standard deviation > min_sd
\item This ensures at least some genetic variation exists
\item Monomorphic variants (all 0s or all same value) are flagged
}
}

\strong{Computational Complexity}: O(n * p) where n=samples, p=variants

\strong{Returns}: List with validation status and diagnostic information
}
\examples{
# Valid genotype matrix (discrete, no missing)
G <- matrix(sample(0:2, 100, replace=TRUE), nrow=10, ncol=10)
result <- validate_genotype_matrix(G)
result$valid  # TRUE

# Genotype matrix with missing values
G_na <- G
G_na[1:3, 1] <- NA
result_na <- validate_genotype_matrix(G_na, allow_na=TRUE)
result_na$n_missing  # 3

# Continuous dosages
G_dosage <- matrix(runif(100, 0, 2), nrow=10, ncol=10)
result_dosage <- validate_genotype_matrix(G_dosage, discrete=FALSE)

# Invalid: all monomorphic
G_mono <- matrix(0, nrow=10, ncol=10)
result_mono <- validate_genotype_matrix(G_mono)
result_mono$valid  # FALSE

}
