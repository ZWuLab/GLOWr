% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pcs_gds.R
\name{plot.glow_pcs}
\alias{plot.glow_pcs}
\title{Plot Method for glow_pcs Objects}
\usage{
\method{plot}{glow_pcs}(x, type = c("scree", "pairs"), n_pairs = 4L, ...)
}
\arguments{
\item{x}{A \code{glow_pcs} object returned by \code{\link{compute_pcs_gds}}.}

\item{type}{Character. Plot type: \code{"scree"} for a scree plot with
individual eigenvalues (bars) and cumulative variance (line), or
\code{"pairs"} for a scatter plot matrix of the first \code{n_pairs}
PCs. Default: \code{"scree"}.}

\item{n_pairs}{Integer. Number of PCs to include in the pairs plot.
Only used when \code{type = "pairs"}. Default: 4.}

\item{...}{Additional graphical parameters passed to the underlying
plot functions.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Produces diagnostic plots for a \code{glow_pcs} object. Dispatches to
either a scree plot or a PC scatter matrix based on the \code{type}
argument.
}
\examples{
\dontrun{
pcs <- compute_pcs_gds(gds_files, n_pcs = 20)

# Scree plot
plot(pcs, type = "scree")

# PC scatter matrix
plot(pcs, type = "pairs", n_pairs = 6)
}

}
