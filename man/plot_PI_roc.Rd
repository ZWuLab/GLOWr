% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_PI_utils.R
\name{plot_PI_roc}
\alias{plot_PI_roc}
\title{Plot ROC Curves for PI Models}
\usage{
plot_PI_roc(
  evaluation_result,
  show_individual = TRUE,
  highlight_ensemble = TRUE,
  col_individual = "gray",
  col_ensemble = "red",
  alpha_individual = 0.3,
  main = NULL,
  ...
)
}
\arguments{
\item{evaluation_result}{Output from \code{evaluate_PI_models()}.}

\item{show_individual}{Logical. Show individual model curves. Default: TRUE.}

\item{highlight_ensemble}{Logical. Highlight ensemble curve. Default: TRUE.}

\item{col_individual}{Color for individual model curves. Default: "gray".}

\item{col_ensemble}{Color for ensemble curve. Default: "red".}

\item{alpha_individual}{Numeric (0-1). Transparency for individual curves.
Default: 0.3.}

\item{main}{Character. Plot title. NULL for default.}

\item{...}{Additional parameters passed to plot().}
}
\value{
Invisible list with ROC coordinates for further customization:
\item{individual}{List of per-model ROC coordinates (fpr, tpr)}
\item{ensemble}{Ensemble ROC coordinates (fpr, tpr)}
}
\description{
Plots ROC (Receiver Operating Characteristic) curves for individual models
and the ensemble using base R graphics.
}
\examples{
\dontrun{
eval_result <- evaluate_PI_models(models, cases, controls)
plot_PI_roc(eval_result)
}

}
