% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_inputs.R
\name{validate_correlation_matrix}
\alias{validate_correlation_matrix}
\title{Validate correlation matrix}
\usage{
validate_correlation_matrix(M, n_variants = NULL, tol = 1e-08)
}
\arguments{
\item{M}{Matrix to validate as correlation matrix}

\item{n_variants}{Expected dimension (optional). If provided, checks that
nrow(M) == ncol(M) == n_variants}

\item{tol}{Numerical tolerance for symmetry and positive definiteness checks.
Default: 1e-8}
}
\value{
A list with elements:
\describe{
\item{valid}{Logical; TRUE if all checks pass}
\item{message}{Character string with validation result or error message}
\item{dimension}{Dimension of the matrix}
\item{is_symmetric}{Logical; TRUE if symmetric within tolerance}
\item{min_eigenvalue}{Smallest eigenvalue (for PSD check)}
\item{warnings}{Character vector of non-fatal warnings}
}
}
\description{
Validates that a matrix can serve as a correlation matrix for variant-set
tests. Correlation matrices are used to account for linkage disequilibrium
among genetic variants.
}
\details{
Checks that a matrix is a valid correlation matrix suitable for use in
GLOW tests. This includes checking symmetry, diagonal values, positive
semi-definiteness, and appropriate dimensions.

\strong{Validation checks performed}:
\enumerate{
\item \strong{Basic structure}:
\itemize{
\item Must be a square matrix
\item Must be numeric
}
\item \strong{Correlation properties}:
\itemize{
\item Must be symmetric (M = t(M) within tolerance)
\item Diagonal must be all 1s (within tolerance)
\item Off-diagonal values must be in \eqn{[-1, 1]}
}
\item \strong{Positive semi-definiteness}:
\itemize{
\item All eigenvalues must be >= -tol
\item Required for valid correlation matrices
\item Small negative eigenvalues (< tol) are acceptable due to numerical error
}
\item \strong{Dimension matching} (if n_variants provided):
\itemize{
\item Matrix dimension must match expected number of variants
}
}

\strong{Computational Complexity}: O(n^3) for eigenvalue check where n=dim(M)

\strong{Returns}: List with validation status and diagnostic information
}
\examples{
# Valid correlation matrix
M <- diag(10)
result <- validate_correlation_matrix(M)
result$valid  # TRUE

# Valid with correlation structure
M_cor <- matrix(0.3, 5, 5) + diag(0.7, 5)
result_cor <- validate_correlation_matrix(M_cor)

# Invalid: not symmetric
M_bad <- matrix(runif(25), 5, 5)
result_bad <- validate_correlation_matrix(M_bad)
result_bad$valid  # FALSE

# Check dimension matching
result_dim <- validate_correlation_matrix(M_cor, n_variants=5)
result_dim$valid  # TRUE

}
